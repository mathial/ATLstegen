{% form_theme form 'bootstrap/bootstrap_4_layout.html.twig' %}

  {% extends "common/layout.html.twig" %}

  {% block body %}

    <h3>Generate Matchups</h3>

    <p>To make it work, you must select only players who have this week a rating in the selected sport. <br>
    <b>When you want to generate some matchups with a new player (never ranked before), pick another player with a similar rating instead !</b></p>

    <div class="form">
      {{ form(form) }}
    </div>

    {% if error %}
    	<p><b>{{ error }}</b></p>
    {% endif %}

      

    {% for match in arrMatch %}
    	{{match}}<br>

    {% endfor %}


  {% endblock %}

